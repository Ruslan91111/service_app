
# sudo docker-compose build

# создать докер контейнер с удалением по окончанию и выполнением через терминал команды о запуске проекта
# sudo docker-compose run --rm web-app sh -c "django-admin startproject service ."

# запустить контейнер
# sudo docker-compose up
